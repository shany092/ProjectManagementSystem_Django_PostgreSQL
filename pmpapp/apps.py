from django.apps import AppConfig



class PmpappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'pmpapp'
    verbose_name = 'Setup' # Hide the app name
    